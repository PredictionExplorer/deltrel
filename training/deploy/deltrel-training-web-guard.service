[Unit]
Description=Keep training-host HTTP and HTTPS ports private
DefaultDependencies=no
After=local-fs.target
Before=network-pre.target
Wants=network-pre.target

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/deltrel-training-web-guard
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
